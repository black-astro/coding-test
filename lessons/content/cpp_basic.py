"""C++ 기초 문법 (스타터 — 더 많은 항목은 추가 가능)."""

from engine.models import Lesson

LESSONS = [

    Lesson(
        id="cpp-basic-01",
        lang="cpp", level="기초",
        title="변수와 기본 타입",
        summary="int / long long / double / char / bool",
        explanation=(
            "━━ 변수와 기본 타입이란? ━━\n"
            "\n"
            "변수(variable)는 값을 담아두는 \"이름 붙은 상자\"예요.\n"
            "마치 냉장고 칸마다 이름표(계란, 우유, 주스)를 붙이는 것처럼,\n"
            "컴퓨터 메모리의 특정 공간에 이름을 붙여서 값을 넣고 꺼낼 수 있어요.\n"
            "\n"
            "C++은 상자를 만들 때 반드시 \"어떤 종류의 값을 담을 상자인지\"를 먼저 알려줘야 해요.\n"
            "이것을 타입(type) 또는 자료형이라고 부르는데, 담을 내용물 종류에 따라 달라져요.\n"
            "\n"
            "기본 타입 종류:\n"
            "  int        →  정수 상자. -21억 ~ +21억까지 담아요 (32비트)\n"
            "  long long  →  더 큰 정수 상자. 약 ±922경까지 담아요 (64비트)\n"
            "  double     →  소수(실수) 상자. 3.14, -2.71 같은 값을 담아요\n"
            "  char       →  글자 하나를 담는 상자. 'A', '0', '!' 같은 문자 1개\n"
            "  bool       →  참/거짓 두 가지만 담는 상자. true(참) 또는 false(거짓)\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  #include <bits/stdc++.h>              →  C++의 모든 기능을 불러오는 마법의 주문이에요. 이 한 줄로 cout, cin 등 모든 기능을 쓸 수 있어요\n"
            "  using namespace std;                  →  표준 기능들을 짧게 쓸 수 있게 해줘요. 이게 없으면 std::cout이라고 길게 써야 해요\n"
            "  int a = 10;                           →  'a'라는 이름의 정수 상자를 만들고 10을 넣어요\n"
            "  long long big = 10000000000LL;        →  'big'이라는 큰 정수 상자를 만들고 100억을 넣어요. 뒤에 LL을 붙여야 long long으로 인식해요\n"
            "  double d = 3.14;                      →  'd'라는 실수 상자를 만들고 3.14를 넣어요\n"
            "  char c = 'A';                         →  'c'라는 문자 상자를 만들고 대문자 A를 넣어요. 문자는 반드시 작은따옴표('')로 감싸요\n"
            "  bool flag = true;                     →  'flag'라는 참/거짓 상자를 만들고 '참'을 넣어요\n"
            "  cout << \"a = \" << a << '\\n';          →  화면에 \"a = 10\"을 출력하고 줄을 바꿔요\n"
            "  cout << boolalpha << \"flag = \" << flag << '\\n';  →  boolalpha를 쓰면 bool 값이 0/1이 아닌 true/false로 출력돼요\n"
            "\n"
            "주의할 점:\n"
            "  int로 선언했는데 21억을 넘는 값을 넣으면 조용히 틀린 값이 돼요 (오버플로 — 상자가 넘쳐 흘러요!)\n"
            "  변수를 만들고 값을 넣지 않으면 쓰레기값(garbage value)이 들어있어요. 반드시 초기화하세요\n"
            "  char 의 값은 반드시 작은따옴표(''), string의 값은 큰따옴표(\"\")로 감싸야 해요"
        ),
        usage="값 저장의 기본. 큰 수가 나오는 문제(누적합, 곱)는 처음부터 long long 을 쓰면 안전하다.",
        cons="타입을 직접 골라야 하고, 범위를 넘으면(오버플로) 경고 없이 값이 망가진다. 초기화를 빠뜨리면 쓰레기값이 들어간다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    // 각 기본 타입을 고정값으로 선언해 본다\n"
            "    int a = 10;            // 정수 (보통 32비트)\n"
            "    long long big = 10000000000LL;  // 32비트 범위를 넘는 큰 정수\n"
            "    double d = 3.14;       // 실수\n"
            "    char c = 'A';          // 문자 하나\n"
            "    bool flag = true;      // 참/거짓\n"
            "\n"
            "    cout << \"a = \" << a << '\\n';\n"
            "    cout << \"big = \" << big << '\\n';\n"
            "    cout << \"d = \" << d << '\\n';\n"
            "    cout << \"c = \" << c << '\\n';\n"
            "    cout << boolalpha << \"flag = \" << flag << '\\n';  // true 로 출력\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-02",
        lang="cpp", level="기초",
        title="표준 입출력 cin / cout",
        summary="cin 으로 입력 · cout 으로 출력",
        explanation=(
            "━━ 표준 입출력 cin / cout 이란? ━━\n"
            "\n"
            "프로그램이 사용자와 \"대화\"하는 방법이에요.\n"
            "cin(씨인)은 키보드로부터 값을 읽어오고,\n"
            "cout(씨아웃)은 화면에 값을 출력해요.\n"
            "\n"
            "실생활로 비유하면:\n"
            "  cin  →  선생님이 질문하면 학생이 대답하는 것 (키보드에서 입력 받기)\n"
            "  cout →  선생님이 칠판에 정답을 써주는 것 (화면에 출력하기)\n"
            "\n"
            "기호 읽는 법:\n"
            "  >>  →  \"입력받아서 변수에 넣어라\" (화살표가 변수를 향해 있어요)\n"
            "  <<  →  \"이 값을 화면으로 내보내라\" (화살표가 cout에서 나가는 방향이에요)\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  ios::sync_with_stdio(false);          →  C++의 입출력 속도를 빠르게 해요. 큰 데이터 입력 문제에서 필수예요\n"
            "  cin.tie(nullptr);                     →  cin과 cout의 강제 연결을 끊어서 더 빠르게 해요. 항상 세트로 써요\n"
            "  cin >> x >> y;                        →  키보드에서 숫자를 두 개 읽어서 x와 y에 넣어요. 공백이나 줄바꿈으로 구분돼요\n"
            "  cout << \"두 수: \" << x << \" \" << y << '\\n';  →  화면에 \"두 수: 3 5\"처럼 출력하고 줄을 바꿔요\n"
            "  cout << \"합: \" << (x + y) << '\\n';  →  x와 y를 더한 결과를 출력해요\n"
            "  cout << x << \" + \" << y << \" = \" << (x + y) << '\\n';  →  \"3 + 5 = 8\" 형태로 출력해요\n"
            "\n"
            "줄바꿈 두 가지 방법:\n"
            "  '\\n'   →  빠른 줄바꿈. 코딩 테스트에서 이걸 써요\n"
            "  endl   →  줄바꿈 + 버퍼 비우기. 느려서 대량 출력에는 비추천\n"
            "\n"
            "주의할 점:\n"
            "  cin >>는 공백(스페이스)이나 줄바꿈(엔터)을 기준으로 값을 끊어 읽어요\n"
            "  공백이 포함된 문장 전체를 읽으려면 cin >>가 아니라 getline(cin, s)를 써야 해요\n"
            "  속도 문제가 있을 때는 항상 ios::sync_with_stdio(false); cin.tie(nullptr);를 main 맨 앞에 넣어요\n"
            "  endl은 줄을 바꿔주지만 느려요. 대신 '\\n'을 쓰는 게 좋아요"
        ),
        usage="모든 입출력의 기본. 백준 같은 표준입력 문제에서 cin 으로 받고 cout 으로 답을 낸다.",
        cons="cin >> 는 줄 단위가 아니라 공백 단위라 한 줄 전체(공백 포함)를 읽으려면 getline 을 따로 써야 한다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    // 표준입력 없이 데모하기 위해 cin 대신 고정값을 사용한다\n"
            "    int x = 3, y = 5;        // 원래라면 cin >> x >> y;\n"
            "    cout << \"두 수: \" << x << \" \" << y << '\\n';\n"
            "    cout << \"합: \" << (x + y) << '\\n';\n"
            "    cout << \"곱: \" << (x * y) << '\\n';\n"
            "\n"
            "    // 여러 값을 한 줄에 이어서 출력\n"
            "    cout << x << \" + \" << y << \" = \" << (x + y) << '\\n';\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-03",
        lang="cpp", level="기초",
        title="조건문과 반복문",
        summary="if / else · for · while",
        explanation=(
            "━━ 조건문과 반복문이란? ━━\n"
            "\n"
            "조건문(if/else)은 \"상황에 따라 다른 길을 선택\"하는 것이에요.\n"
            "마치 갈림길에서 \"비가 오면 우산을 챙기고, 맑으면 그냥 나간다\"처럼요.\n"
            "\n"
            "반복문(for/while)은 \"같은 일을 여러 번 반복\"하는 것이에요.\n"
            "\"1부터 100까지 숫자를 모두 더해라\"를 100번 손으로 쓰는 대신,\n"
            "반복문으로 컴퓨터가 빠르게 처리하게 해요.\n"
            "\n"
            "[조건문 if / else if / else]\n"
            "  if (조건)        →  조건이 참이면 이 블록 실행\n"
            "  else if (조건2)  →  첫 조건이 거짓이고 조건2가 참이면 이 블록 실행\n"
            "  else             →  모든 조건이 거짓이면 이 블록 실행\n"
            "\n"
            "[반복문 for]\n"
            "  for (시작; 계속조건; 변화) { 반복할 내용 }\n"
            "  예: for (int i = 1; i <= 10; i++)\n"
            "      → i를 1로 시작해서, i가 10이하인 동안, 매번 i를 1 증가시키며 반복\n"
            "\n"
            "[반복문 while]\n"
            "  while (조건) { 반복할 내용 }\n"
            "  → 조건이 참인 동안 계속 반복. 조건이 거짓이 되는 순간 멈춰요\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  int total = 0;                  →  합계를 저장할 상자를 만들고 0으로 초기화해요 (초기화 안 하면 쓰레기값!)\n"
            "  for (int i = 1; i <= 10; i++) { →  i를 1부터 10까지 1씩 늘리며 반복해요 (총 10번)\n"
            "      if (i % 2 == 0) {           →  i를 2로 나눈 나머지가 0이면 짝수예요. %는 나머지 연산자예요\n"
            "          total += i;             →  total = total + i 와 같아요. 짝수일 때만 total에 더해요\n"
            "  cout << \"짝수합: \" << total;    →  결과(30)를 출력해요. 2+4+6+8+10=30\n"
            "  int n = 5;                      →  n이라는 상자에 5를 넣어요\n"
            "  while (n > 0) {                 →  n이 0보다 큰 동안 계속 반복해요\n"
            "      cout << n << ' ';           →  현재 n값을 출력하고 공백을 출력해요\n"
            "      n--;                        →  n = n - 1 과 같아요. n을 1 줄여요\n"
            "  }                               →  n이 0이 되면 while 조건이 거짓이 되어 반복 종료\n"
            "\n"
            "주의할 점:\n"
            "  while 안에서 조건을 변화시키지 않으면 영원히 반복하는 무한 루프가 돼요! 항상 탈출 조건을 만들어야 해요\n"
            "  for의 i는 보통 0부터 시작하는 경우가 많아요 (배열 인덱스가 0부터 시작하기 때문)\n"
            "  break를 쓰면 반복을 즉시 완전히 종료해요\n"
            "  continue를 쓰면 현재 회차를 건너뛰고 다음 회차로 가요\n"
            "  i % 2 == 0 에서 == 를 = 로 쓰면 비교가 아니라 대입이 되어 버그가 나요!"
        ),
        usage="모든 흐름 제어의 기본. 합 구하기, 개수 세기, 조건 분기 등 거의 모든 문제에 등장한다.",
        cons="반복 조건이나 증감을 잘못 쓰면 무한 루프에 빠진다. 인덱스 범위를 넘으면 배열에서 오류가 난다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    // 1~10 중 짝수의 합을 구한다\n"
            "    int total = 0;\n"
            "    for (int i = 1; i <= 10; i++) {\n"
            "        if (i % 2 == 0) {      // 짝수만 더한다\n"
            "            total += i;\n"
            "        }\n"
            "    }\n"
            "    cout << \"짝수합: \" << total << '\\n';   // 30\n"
            "\n"
            "    // while 로 같은 수를 거꾸로 출력\n"
            "    int n = 5;\n"
            "    while (n > 0) {\n"
            "        cout << n << ' ';\n"
            "        n--;\n"
            "    }\n"
            "    cout << '\\n';   // 5 4 3 2 1\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-04",
        lang="cpp", level="기초",
        title="배열과 vector 기초",
        summary="고정 배열 · 가변 배열 vector",
        explanation=(
            "━━ 배열과 vector란? ━━\n"
            "\n"
            "배열(array)은 같은 타입의 값을 여러 개 나란히 담는 \"칸막이 상자\"예요.\n"
            "달걀판을 생각해보세요 — 크기가 딱 정해져 있고 (6칸, 12칸 등), 나중에 칸을 늘릴 수 없어요.\n"
            "배열도 이와 같아요: 처음에 크기를 정해두면 나중에 변경할 수 없어요.\n"
            "\n"
            "vector는 크기를 자유롭게 늘릴 수 있는 \"마법의 가방\"이에요.\n"
            "처음에 비어있다가 push_back()으로 값을 하나씩 추가할 수 있고, 크기 제한이 없어요.\n"
            "코딩 테스트에서는 입력 개수가 미리 정해지지 않은 경우가 많아서 vector를 자주 써요.\n"
            "\n"
            "인덱스(index)란?:\n"
            "  배열/vector의 각 칸에는 번호가 있어요. 0번부터 시작해요!\n"
            "  5칸짜리 배열이면 0, 1, 2, 3, 4번 칸이 있어요 (5번 칸은 없어요!)\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  int arr[5] = {5, 2, 9, 1, 7};          →  5칸짜리 정수 배열을 만들고 5개의 값을 순서대로 넣어요\n"
            "  int sum = 0;                            →  합계를 저장할 sum을 0으로 초기화해요\n"
            "  for (int i = 0; i < 5; i++) sum += arr[i];  →  i를 0,1,2,3,4로 바꾸며 arr[0]+arr[1]+...+arr[4]를 계산해요\n"
            "  cout << \"배열 합: \" << sum << '\\n';    →  합계(24)를 출력해요 (5+2+9+1+7=24)\n"
            "  vector<int> v;                          →  정수를 담는 빈 vector를 만들어요. <int>는 정수 타입을 담겠다는 뜻이에요\n"
            "  v.push_back(3);                         →  vector의 맨 뒤에 3을 추가해요. 가방에 물건 하나 넣기!\n"
            "  v.push_back(1);                         →  vector에 1을 추가해요\n"
            "  v.push_back(2);                         →  vector에 2를 추가해요. 이제 v = [3, 1, 2]\n"
            "  sort(v.begin(), v.end());               →  v의 처음부터 끝까지 오름차순으로 정렬해요. 정렬 후 v = [1, 2, 3]\n"
            "  for (int x : v) cout << ' ' << x;      →  v의 각 원소를 x에 담아서 하나씩 출력해요. \"범위 기반 for\"라고 해요\n"
            "  cout << \"크기: \" << v.size() << '\\n'; →  vector에 들어있는 원소 개수(3)를 출력해요\n"
            "\n"
            "주의할 점:\n"
            "  배열 인덱스는 0부터! arr[5]가 선언되면 arr[0]~arr[4]까지만 써요. arr[5]에 접근하면 범위 초과 에러!\n"
            "  vector.size()의 반환 타입은 부호 없는 정수(size_t)예요. int i와 비교할 때 (int)v.size()로 형변환하면 안전해요\n"
            "  sort()를 쓰려면 #include <algorithm> 이 필요해요 (bits/stdc++.h로 한번에 해결 가능)\n"
            "  vector는 = 로 복사할 수 있어요: vector<int> v2 = v;"
        ),
        usage="여러 값을 모아 다룰 때 기본. 입력 개수가 가변이면 vector 가 편하고 안전하다.",
        cons="고정 배열은 범위를 넘어 접근해도 막아주지 않아 위험하다. vector 도 v[i] 는 범위 검사를 하지 않는다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    // 고정 크기 배열\n"
            "    int arr[5] = {5, 2, 9, 1, 7};\n"
            "    int sum = 0;\n"
            "    for (int i = 0; i < 5; i++) sum += arr[i];\n"
            "    cout << \"배열 합: \" << sum << '\\n';   // 24\n"
            "\n"
            "    // 가변 배열 vector\n"
            "    vector<int> v;\n"
            "    v.push_back(3);\n"
            "    v.push_back(1);\n"
            "    v.push_back(2);\n"
            "    sort(v.begin(), v.end());   // 오름차순 정렬\n"
            "\n"
            "    cout << \"정렬된 vector:\";\n"
            "    for (int x : v) cout << ' ' << x;   // 1 2 3\n"
            "    cout << '\\n';\n"
            "    cout << \"크기: \" << v.size() << '\\n';\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-05",
        lang="cpp", level="기초",
        title="string 기초",
        summary="std::string · 연결 · 길이 · 부분 문자열",
        explanation=(
            "━━ string이란? ━━\n"
            "\n"
            "string은 여러 글자(문자들)의 모음, 즉 \"문자열\"을 저장하는 타입이에요.\n"
            "\"Hello\", \"나는 학생입니다\", \"12345\" 같은 글자들의 연속이에요.\n"
            "\n"
            "실생활로 비유하면:\n"
            "  char (문자 하나) → 레고 블록 하나\n"
            "  string (문자열)  → 레고 블록 여러 개를 연결한 것\n"
            "\n"
            "string vs char 배열:\n"
            "  C언어 스타일의 char[] 배열도 있지만 string이 훨씬 편하고 안전해요.\n"
            "  + 로 이어붙이고, 길이도 쉽게 구할 수 있고, 비교도 == 로 가능해요.\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  string a = \"Hello\";                       →  a라는 문자열 상자에 \"Hello\"를 넣어요\n"
            "  string b = \"World\";                       →  b라는 문자열 상자에 \"World\"를 넣어요\n"
            "  string c = a + \", \" + b + \"!\";           →  문자열들을 + 로 이어붙여요. c = \"Hello, World!\"\n"
            "  cout << c << '\\n';                        →  c 전체를 출력해요\n"
            "  cout << \"길이: \" << c.size() << '\\n';   →  c의 문자 개수(13)를 출력해요. length()를 써도 같아요\n"
            "  cout << \"첫 글자: \" << c[0] << '\\n';    →  c[0]은 첫 번째 글자 'H'예요. 인덱스는 0부터!\n"
            "  cout << c.substr(7, 5) << '\\n';           →  7번 인덱스부터 5글자를 잘라요. \"World\" 출력\n"
            "  string r = a;                              →  r에 a의 내용을 복사해요 (r = \"Hello\")\n"
            "  reverse(r.begin(), r.end());               →  r의 내용을 앞뒤로 뒤집어요. r = \"olleH\"\n"
            "  cout << \"뒤집기: \" << r << '\\n';        →  뒤집힌 결과 \"olleH\"를 출력해요\n"
            "\n"
            "자주 쓰는 string 기능 정리:\n"
            "  s.size() / s.length()    →  문자 개수 반환\n"
            "  s[i]                     →  i번째 글자 접근 (0부터 시작)\n"
            "  s.substr(시작, 길이)     →  시작 인덱스부터 길이만큼 잘라서 반환\n"
            "  s.substr(시작)           →  시작 인덱스부터 끝까지 반환\n"
            "  s.find(\"찾을것\")         →  처음 등장하는 인덱스 반환. 없으면 string::npos\n"
            "  s += \"추가\";            →  문자열 뒤에 덧붙이기\n"
            "  reverse(s.begin(), s.end());  →  문자열 뒤집기\n"
            "\n"
            "주의할 점:\n"
            "  string은 큰따옴표(\"\"), char는 작은따옴표('')를 써요. 'Hello'는 에러예요!\n"
            "  s[i]로 접근할 때 범위를 넘으면 위험해요. 안전하게 쓰려면 s.at(i)를 써요 (범위 초과 시 예외 발생)\n"
            "  공백 포함 한 줄 전체를 읽으려면 getline(cin, s)를 써요\n"
            "  큰 문자열을 반복 + 로 이어붙이면 느려져요. += 를 쓰거나 stringstream을 활용해요"
        ),
        usage="이름/문장 처리, 문자열 뒤집기, 부분 문자열 탐색 등 문자열 문제 전반에 쓴다.",
        cons="큰 문자열을 + 로 반복해서 이어붙이면 복사가 잦아 느려질 수 있다. += 나 reserve 로 최적화한다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    string a = \"Hello\";\n"
            "    string b = \"World\";\n"
            "    string c = a + \", \" + b + \"!\";   // 문자열 연결\n"
            "\n"
            "    cout << c << '\\n';                 // Hello, World!\n"
            "    cout << \"길이: \" << c.size() << '\\n';\n"
            "    cout << \"첫 글자: \" << c[0] << '\\n';\n"
            "    cout << \"부분: \" << c.substr(7, 5) << '\\n';   // World\n"
            "\n"
            "    // 문자열 뒤집기\n"
            "    string r = a;\n"
            "    reverse(r.begin(), r.end());\n"
            "    cout << \"뒤집기: \" << r << '\\n';   // olleH\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-06",
        lang="cpp", level="기초",
        title="형변환과 정수 오버플로",
        summary="int ↔ double · long long 으로 오버플로 방지",
        explanation=(
            "━━ 형변환과 정수 오버플로란? ━━\n"
            "\n"
            "[형변환 (Type Casting)]\n"
            "형변환은 한 타입의 값을 다른 타입으로 바꾸는 것이에요.\n"
            "실생활 비유: kg 단위로 적힌 무게를 g 단위로 바꾸는 것처럼요.\n"
            "\n"
            "가장 중요한 경우: 정수 ÷ 정수 = 정수 (소수점이 잘려나가요!)\n"
            "  7 / 2 → 3 (3.5가 아니에요!)\n"
            "  왜냐하면 두 피연산자가 모두 int이면 결과도 int이기 때문이에요.\n"
            "  실수 결과를 원하면 적어도 한 쪽을 double로 바꿔야 해요.\n"
            "\n"
            "형변환 방법:\n"
            "  (double)x              →  x를 double로 바꿔요 (C 스타일, 간단하고 많이 씀)\n"
            "  static_cast<double>(x) →  C++ 스타일. 더 안전하고 명확해요\n"
            "\n"
            "[정수 오버플로 (Integer Overflow)]\n"
            "오버플로는 상자(변수)에 담을 수 있는 크기를 넘어서 값이 망가지는 것이에요.\n"
            "물컵에 물이 가득 찼는데 더 부으면 넘치는 것처럼요.\n"
            "\n"
            "  int의 최대값: 약 21억 (2,147,483,647)\n"
            "  이를 넘으면 갑자기 음수나 엉뚱한 값이 돼요! (에러 없이 조용히!)\n"
            "  long long 최대값: 약 922경 (9,223,372,036,854,775,807)\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  int a = 7, b = 2;                       →  a에 7, b에 2를 넣어요\n"
            "  cout << (a / b) << '\\n';                →  int ÷ int = int이므로 3이 출력돼요 (0.5 버림)\n"
            "  cout << ((double)a / b) << '\\n';        →  a를 double로 바꾼 뒤 나누면 3.5가 출력돼요\n"
            "  int x = 100000;                         →  x에 10만을 넣어요\n"
            "  long long good = (long long)x * x;      →  먼저 x를 long long으로 올린 뒤 곱해요. 결과: 100억\n"
            "  (만약 long long good = x * x; 라면)     →  int끼리 곱해서 오버플로가 먼저 일어나요! (위험!)\n"
            "  long long sum = 0;                      →  합계를 long long으로 선언해요 (누적하면 int를 넘을 수 있어요)\n"
            "  for (int i = 1; i <= 1000000; i++) sum += i;  →  1부터 100만까지 모두 더해요\n"
            "  cout << sum << '\\n';                    →  결과(500,000,500,000)를 출력해요\n"
            "\n"
            "주의할 점:\n"
            "  오버플로는 에러 메시지 없이 조용히 발생해요! 틀린 답이 나오지만 이유를 찾기 어려워요\n"
            "  곱셈 중간에도 오버플로가 나요: int x = 100000; x * x는 이미 int 범위를 초과해요\n"
            "  long long에 큰 값을 직접 쓸 때 숫자 뒤에 LL을 붙여요: 10000000000LL (안 붙이면 int로 처리돼요)\n"
            "  평균을 구할 때 (a + b) / 2 는 a+b가 오버플로될 수 있어요. long long으로 먼저 변환하세요"
        ),
        usage="평균 계산(실수 변환), 큰 수 곱/합 문제(long long)에서 정확한 답을 내기 위해 필수다.",
        cons="형변환을 빠뜨리면 조용히 틀린 답이 나온다(에러 없이). 어디서 int 곱이 일어나는지 늘 신경 써야 한다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    int a = 7, b = 2;\n"
            "    cout << \"정수 나눗셈: \" << (a / b) << '\\n';        // 3 (소수점 버림)\n"
            "    cout << \"실수 나눗셈: \" << ((double)a / b) << '\\n'; // 3.5\n"
            "\n"
            "    // 오버플로 예시\n"
            "    int x = 100000;\n"
            "    long long good = (long long)x * x;  // 먼저 long long 으로 올려 곱한다\n"
            "    cout << \"올바른 곱: \" << good << '\\n';   // 10000000000\n"
            "\n"
            "    // 큰 누적합도 long long 으로 안전하게\n"
            "    long long sum = 0;\n"
            "    for (int i = 1; i <= 1000000; i++) sum += i;\n"
            "    cout << \"1~100만 합: \" << sum << '\\n';   // 500000500000\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-07",
        lang="cpp", level="기초",
        title="연산자",
        summary="산술 · 비교 · 논리 · 복합대입",
        explanation=(
            "━━ 연산자란? ━━\n"
            "\n"
            "연산자(operator)는 값들 사이에서 계산이나 비교를 수행하는 기호예요.\n"
            "수학의 +, -, ×, ÷ 기호처럼, C++에도 다양한 연산 기호가 있어요.\n"
            "\n"
            "[산술 연산자] — 계산을 해요\n"
            "  +   →  더하기. 5 + 3 = 8\n"
            "  -   →  빼기.   5 - 3 = 2\n"
            "  *   →  곱하기. 5 * 3 = 15\n"
            "  /   →  나누기. 5 / 2 = 2 (정수 나눗셈은 소수점 버림!)\n"
            "  %   →  나머지. 17 % 5 = 2 (5를 3번 쓰고 2가 남아요)\n"
            "\n"
            "[비교 연산자] — 두 값을 비교해서 true/false를 반환해요\n"
            "  ==  →  같으면 true.           a == b\n"
            "  !=  →  다르면 true.           a != b\n"
            "  <   →  왼쪽이 작으면 true\n"
            "  <=  →  왼쪽이 작거나 같으면 true\n"
            "  >   →  왼쪽이 크면 true\n"
            "  >=  →  왼쪽이 크거나 같으면 true\n"
            "\n"
            "[논리 연산자] — 조건들을 조합해요\n"
            "  &&  →  그리고(AND). 둘 다 true여야 true. \"오늘 비가 오고 && 우산이 없으면 젖어요\"\n"
            "  ||  →  또는(OR).  하나라도 true면 true. \"버스나 || 지하철이면 탈 수 있어요\"\n"
            "  !   →  부정(NOT). true→false, false→true. \"배고프지 않으면 밥 안 먹어요\"\n"
            "\n"
            "[복합 대입 연산자] — 계산과 대입을 동시에\n"
            "  +=  →  a += 5 는 a = a + 5 와 같아요\n"
            "  -=  →  a -= 3 는 a = a - 3 과 같아요\n"
            "  *=  →  a *= 2 는 a = a * 2 와 같아요\n"
            "  ++  →  a++ 는 a = a + 1 과 같아요 (1 증가)\n"
            "  --  →  a-- 는 a = a - 1 과 같아요 (1 감소)\n"
            "\n"
            "[삼항 연산자] — if/else를 한 줄로\n"
            "  조건 ? 참일때값 : 거짓일때값\n"
            "  (a > b) ? a : b  →  a가 b보다 크면 a, 아니면 b를 반환해요 (더 큰 값 선택)\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  int a = 17, b = 5;                              →  a에 17, b에 5를 넣어요\n"
            "  cout << \"a + b = \" << (a + b) << '\\n';        →  17 + 5 = 22를 출력해요\n"
            "  cout << \"a / b = \" << (a / b) << '\\n';        →  17 ÷ 5 = 3.4인데 정수 나눗셈이라 3이 돼요\n"
            "  cout << \"a % b = \" << (a % b) << '\\n';        →  17을 5로 나누면 몫이 3이고 나머지가 2예요\n"
            "  cout << (a > b && b > 0) << '\\n';              →  17>5가 true이고 5>0도 true이므로 true\n"
            "  int mx = (a > b) ? a : b;                       →  a(17)가 b(5)보다 크니까 mx = 17이 돼요\n"
            "  int cnt = 0;                                     →  cnt를 0으로 초기화해요\n"
            "  cnt += 10;                                       →  cnt = 0 + 10 = 10\n"
            "  cnt++;                                           →  cnt = 10 + 1 = 11\n"
            "  cout << \"cnt = \" << cnt << '\\n';              →  11을 출력해요\n"
            "\n"
            "주의할 점:\n"
            "  == 와 = 를 헷갈리지 마세요! if (a = 5)는 비교가 아니라 대입(5를 넣음)이에요. 조건은 if (a == 5)!\n"
            "  && 는 양쪽 모두 true여야 true고, || 는 하나만 true여도 true예요\n"
            "  % 나머지 연산은 짝수/홀수 판별(i%2==0), 배수 판별(i%3==0), 자릿수 추출에 자주 써요\n"
            "  / 나눗셈은 정수끼리면 소수점이 버려져요. 실수 결과가 필요하면 (double)으로 형변환하세요"
        ),
        usage="모든 계산과 조건 판단의 기본 도구. 짝수 판별, 최댓값 선택 등에 두루 쓴다.",
        cons="&& 와 ||, == 와 = 를 헷갈리면 미묘한 버그가 난다. 정수끼리의 / 는 나머지를 버린다는 점도 주의.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "int main() {\n"
            "    int a = 17, b = 5;\n"
            "    cout << \"a + b = \" << (a + b) << '\\n';   // 22\n"
            "    cout << \"a / b = \" << (a / b) << '\\n';   // 3\n"
            "    cout << \"a % b = \" << (a % b) << '\\n';    // 2\n"
            "\n"
            "    // 비교와 논리 (boolalpha 로 true/false 출력)\n"
            "    cout << boolalpha;\n"
            "    cout << \"a > b 그리고 b > 0: \" << (a > b && b > 0) << '\\n';  // true\n"
            "\n"
            "    // 삼항 연산자로 더 큰 값 고르기\n"
            "    int mx = (a > b) ? a : b;\n"
            "    cout << \"더 큰 값: \" << mx << '\\n';   // 17\n"
            "\n"
            "    // 복합 대입과 증감\n"
            "    int cnt = 0;\n"
            "    cnt += 10;\n"
            "    cnt++;\n"
            "    cout << \"cnt = \" << cnt << '\\n';   // 11\n"
            "    return 0;\n"
            "}\n"
        ),
    ),

    Lesson(
        id="cpp-basic-08",
        lang="cpp", level="기초",
        title="함수 정의",
        summary="반환형 · 매개변수 · 참조 전달",
        explanation=(
            "━━ 함수란? ━━\n"
            "\n"
            "함수(function)는 \"특정 작업을 수행하는 코드 묶음\"에 이름을 붙인 것이에요.\n"
            "마치 요리 레시피처럼 — 한 번 작성해두면 언제든지 레시피 이름만 불러서 쓸 수 있어요.\n"
            "\n"
            "왜 함수를 쓰나요?\n"
            "  1. 재사용: 같은 코드를 여러 번 복붙할 필요 없이 함수를 호출하면 돼요\n"
            "  2. 가독성: 코드가 짧고 깔끔해져요\n"
            "  3. 유지보수: 수정이 필요할 때 함수 하나만 고치면 돼요\n"
            "\n"
            "함수의 구조:\n"
            "  반환형 함수이름(매개변수) {\n"
            "      // 실행할 코드\n"
            "      return 반환값;\n"
            "  }\n"
            "\n"
            "  반환형: 함수가 돌려주는 값의 타입 (int, double, string 등)\n"
            "           돌려줄 값이 없으면 void를 써요\n"
            "  매개변수: 함수에 전달하는 입력값 (없어도 됨)\n"
            "\n"
            "[값 전달 vs 참조 전달]\n"
            "  값 전달 (call by value): 변수의 복사본을 넘겨요. 원본은 그대로예요\n"
            "    → 마치 서류를 복사해서 넘기는 것처럼요. 원본 서류는 안전!\n"
            "  참조 전달 (call by reference): 원본 변수 자체를 넘겨요. 원본이 바뀌어요\n"
            "    → & 기호를 붙여요. 마치 원본 서류를 직접 넘기는 것처럼요.\n"
            "\n"
            "코드 한 줄씩 읽기:\n"
            "  int add(int a, int b) {          →  int를 반환하는 add 함수예요. a와 b라는 정수를 입력으로 받아요\n"
            "      return a + b;                →  a + b 의 결과를 반환해요. 함수를 호출한 곳으로 값이 돌아가요\n"
            "  void doubleIt(int& x) {          →  반환값이 없는(void) 함수예요. &는 참조로 받겠다는 뜻이에요\n"
            "      x *= 2;                      →  x를 두 배로 만들어요. 참조라서 원본 변수가 직접 바뀌어요\n"
            "  int maxOf(int a, int b) {        →  두 int를 받아서 더 큰 int를 반환하는 함수예요\n"
            "      return (a > b) ? a : b;      →  삼항 연산자로 더 큰 값을 반환해요\n"
            "  cout << add(3, 4) << '\\n';      →  add 함수에 3과 4를 주면 7을 돌려줘서 7이 출력돼요\n"
            "  int n = 10;                      →  n에 10을 넣어요\n"
            "  doubleIt(n);                     →  n을 참조로 넘기니까 함수 안에서 n 원본이 바뀌어요\n"
            "  cout << \"n = \" << n << '\\n';  →  n은 이제 20이에요 (원본이 바뀌었으니까요)\n"
            "\n"
            "주의할 점:\n"
            "  함수를 main보다 아래에 정의하면 컴파일 에러가 나요. main 위에 정의하거나 앞에 선언(프로토타입)을 해야 해요\n"
            "    → int add(int a, int b); 처럼 함수 원형만 먼저 써두면 main 아래에 정의해도 괜찮아요\n"
            "  큰 vector나 string을 값으로 전달하면 복사 비용이 커요. const 참조(const vector<int>& v)로 넘기는 게 효율적이에요\n"
            "  void 함수에서도 return; 을 쓸 수 있어요 (값 없이). 조기에 함수를 종료할 때 유용해요"
        ),
        usage="반복되는 계산을 묶어 재사용한다. 코드가 짧고 명확해지며 디버깅이 쉬워진다.",
        cons="값 전달은 큰 객체에서 복사 비용이 크다(이럴 땐 const 참조 사용). 함수 호출 자체에도 약간의 비용이 있다.",
        code=(
            "#include <bits/stdc++.h>\n"
            "using namespace std;\n"
            "\n"
            "// 두 수의 합을 반환하는 함수\n"
            "int add(int a, int b) {\n"
            "    return a + b;\n"
            "}\n"
            "\n"
            "// 참조로 받아 원본 값을 직접 두 배로 만든다\n"
            "void doubleIt(int& x) {\n"
            "    x *= 2;\n"
            "}\n"
            "\n"
            "// 최댓값을 돌려주는 함수\n"
            "int maxOf(int a, int b) {\n"
            "    return (a > b) ? a : b;\n"
            "}\n"
            "\n"
            "int main() {\n"
            "    cout << \"add(3, 4) = \" << add(3, 4) << '\\n';   // 7\n"
            "    cout << \"maxOf(8, 5) = \" << maxOf(8, 5) << '\\n'; // 8\n"
            "\n"
            "    int n = 10;\n"
            "    doubleIt(n);          // 참조 전달이라 원본이 바뀐다\n"
            "    cout << \"n = \" << n << '\\n';   // 20\n"
            "    return 0;\n"
            "}\n"
        ),
    ),
]
